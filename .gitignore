/target
# ★ `vendor/pingora` 是**另一个 workspace**：对它直接跑 cargo（不带 --target-dir）会在
#   `vendor/pingora/target/` 下堆出几百 MB，而上面那条 `/target` 锚定根目录、盖不住它。
#   所以再加一条不锚定的，任何层级的 target/ 都盖住。
target/
**/*.rs.bk

# 运行期产物（进程、套接字、日志），不入库
/run/
*.pid
*.sock

# Python 工具的字节码。★ 直接跑脚本不会产生它，但**任何 import 都会**。
__pycache__/
*.py[cod]

# 工作过程记录（交接、草稿）只留在本地，不入库（G115）
/handoff/

# 对拍的输出目录（`bench/run.sh` 的缺省落点）。
# ⚠ ⚠ 挡在这里的只是**缺省落点**，⛔ 不是「对拍的原始数据不入库」——
#   G19 要求基准脚本与**原始数据**全部公开。真要发布的那一趟应当写到一个
#   指定目录再单独提交，⛔ 别把这条 ignore 读成一条数据保密规定。
/bench/out/
