# 枢衡 · 缓存命中 p99 诊断的被测配置（**磁盘后端**那一份，M3，G150 ②）。
#
# ★ ★ ★ **内存那一份由本文件删掉 `disk` 那一行派生**，别的一个字节不改
#   —— 单变量由**生成方式**保证，⛔ 不靠人盯着两份文件对齐。
#   ★ 这是 `tests/cache/disk.sh:145-146` 已经在用的做法，照搬。
#
# ⚠ ⚠ 两个变体**不可能同时存在于一个进程里**：缓存后端是进程级的，多个 `cache`
#   块写不同的 `disk`（含「一个写了、一个没写」）是编译期错误 `FUL-DSL-0035`
#   （`docs/architecture/dsl-reference.md` 的 `cache` 子块表）。
#   ⇒ **背靠背是唯一形状**，不是偷懒。
#
# ⚠ **有意不写 `capacity` 与 `max_size`**，用缺省（256MB / 8MB）：被测只有一个
#   `BENCH_PAYLOAD_BYTES`（缺省 4096）字节的条目，既进得了 `max_size`，
#   也**永远不会触发 LRU 淘汰** ⇒ 这一格量的是纯命中，⛔ 不掺淘汰的代价。
#   ★ 顺带：`capacity` 写了 `disk` 时是**磁盘**预算、不写时是**内存**预算 ——
#     同一条指令两种介质，⇒ 两个变体若都显式写它，它的含义在两边并不相同。
#     不写它，这处不对等就**结构性地不存在**。
#
# ⚠ `ttl` 由 `BENCH_DURATION` **算出来**（10 倍），⛔ 不是字面量 ——
#   理由见 `bench/diag/cache-hit-p99.sh` 里 `derive_ttl` 那段注释（G147 那条形状）。
#   ★ 它必须**远大于**一趟负载的时长：中途过期的话量到的是 `REVALIDATED` 那条
#     **要回源**的路而不是命中，而 oha 的 JSON 里看不出这件事。
#
# ⚠ 占位符由 bench/diag/cache-hit-p99.sh 替换。
{
    # ★ L7 数据面的 worker 数（G140）。与源站及另一个变体吃**同一个** `BENCH_WORKERS`。
    threads_l7 __WORKERS__
}

:__PORT__ {
    cache {
        ttl __TTL__
        disk __CACHE_DIR__
    }
    reverse_proxy 127.0.0.1:__ORIGIN_PORT__
}
