# 这份数据里被改过的**唯一**一处

> ★ 本仓是公开仓。`env.json` 里的 `attest` 是**人写下来的声明原文**，快照按设计把它
> 逐字记下来。这一趟那句原文同样点名了**其它项目**的服务 ⇒ 按 owner 2026-09-06
> 为上一组定下的同一条规则处理：**入库，但声明改成不点名**，并把改了什么明写在这里。
>
> ⛔ **不静默改。** 一份被悄悄修饰过的记录，与一份编造的记录无法区分。

## 改了什么

| | |
|---|---|
| 字段 | `env.json` → `attest` |
| 其余字段 | ⛔ **一个字都没动**（含 `qualified` / `disqualifiers` / `load_params` / 全部读数 / `raw/` 与 `diag/` 下的全部原始数据） |
| 标记 | `env.json` 末尾追加了 `"attest_redacted": true` ⇒ 机器读得出来这份声明被改过 |
| 做法 | **文本替换恰好那一行**，再在末尾追加那一个键。⛔ 没有重新序列化 —— 那会让别的字段的字节（键序、缩进、转义）跟着变，上一行那句「一个字都没动」就不成立了 |

**入库的这一版**（与 `env.json` 里逐字相同）：

```text
专机：该机上全部应用服务与数据存储（共 6 个） 已全部停止（owner 2026-09-10 授权）· 无 TUN：网络设备只有 lo/eth0/docker0（docker0 是容器桥，非代理）· 仍在跑：一个出站隧道客户端、云厂商的两个探针、系统基础服务（chrony/cron/rsyslog/tuned/unattended-upgrades）—— 它们仍会偶尔占用 CPU
```

**与运行时实际声明的那一版相比，差别只有「具名 → 计数／角色」这一类，共三处：**

| 运行时原文 | 入库版 |
|---|---|
| 被停掉的 6 个服务，逐个具名 | 该机上全部应用服务与数据存储（共 6 个） |
| 一个隧道客户端，具名 | 一个出站隧道客户端 |
| 两个云厂商探针，具名 | 云厂商的两个探针 |

★ **这句是机械核过的，⛔ 不是自述**：对原文依次做这三处替换，结果与入库版**逐字相等**，
连标点与空格都算在内 —— 紧跟「（共 6 个）」的那个空格就是原文里的，⛔ 没有为了好看删掉它；
⛔ 也没有顺手补一句原文里没有的话。完整原文留在本地运行记录里，⛔ 不入公开仓。

## ★ 为什么这样改不削弱这句声明

`attest` 那一格要回答的是「**这台机器有没有在承载真业务**」。它的信息量在于
「有几个、停没停、还剩什么在跑」—— 这三件在入库版里一件都没少。
被去掉的是**谁**在跑，而那与这句声明的强度无关。

## ⚠ 这份声明仍然只有纪律，没有门

「专机」与「无 TUN 代理」两件**仍然是容器原理上看不见的**（G145 只把内核参数那件拆成了门）。
⛔ **声明不是证明**，能做到的只有「谁都没声明过就一定不算合格」。
