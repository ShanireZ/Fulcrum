---
type: 项目治理
title: 实施路线（M0–M4）
description: 五个里程碑按「能不能真用」切分；每个的退出条件都是可判定的事实，不是主观进度。
resource: ../../PLAN.md
tags: [治理, 里程碑, 必读]
status: stable
generated:
  by: claude-code/opus-5
  at: 2026-08-12T00:00:00Z
sources:
  - id: plan-7
    resource: /references/plan.md
    title: PLAN.md §7 里程碑（M0–M4 与各自的退出条件）
  - id: plan-10
    resource: /references/plan.md
    title: PLAN.md §10 G24（里程碑按「能自用」切分）、G27、G28
---

★ **完整表述在 [`PLAN.md`](../../PLAN.md) §7。** 判据只有一个（G24）：**能不能真用。**

# 当前位置

**M2 进行中。** 自研批次全部做完（L4 面 · 静态文件 · 缓存 · 压缩 · HTTP/3 · 换代时的
QUIC 跨进程转交 · 结构化访问日志 · Prometheus 指标），剩下的两件是**观测三件套的最后 1/3**
（Runtime 实时 stats，批 N）与**退出条件本身**（两台服务器全部由枢衡承载）。

仓库里有**六个产品 crate**（`fulcrum-config` / `fulcrum-runtime` / `fulcrum-tls` /
`fulcrum-acme` / `fulcrum-server` / `fulcrum`）与**两个 spike**（`spikes/m0-seam`、
`spikes/m1-systemd`）。★ **spike 不是产品代码**，是为了回答一个问题而写的验证装置 ——
⚠ 而「一个 spike 证明了这条路走得通」与「产品走在这条路上」是两件事：
systemd 托管的曾经是 spike，而产品从没接过 `sd_notify`，就是这么漏过去的。

# 五个里程碑

| | 名称 | 状态 | 退出条件（可判定的事实） |
|---|---|---|---|
| **M0** | 接缝验证 | ✅ **通过** | 一次 `SIGQUIT` + `-u` 优雅升级中，HTTP / TCP 长连接 / UDP 回声**三类零中断**，且能在 Docker 里一条命令重跑 |
| **M1** | 接管一台机 | ✅ **通过** | 一台真实服务器由枢衡承载、跑通冒烟 + 压力（G73） |
| **M2** | 接管两台 | ⏳ **进行中** | **两台**服务器全部由枢衡承载；回落层已无常态使用（G81） |
| **M3** | 对拍达标 | ⏳ 未开工 | 每类用例不劣于**该类最强者** 10%；基准脚本与原始数据**可被第三方复现** |
| **M4** | 发布 | ⏳ 未开工 | 已在生产**连续承载 ≥ 90 天**，且 M3 达标 |

# 每个里程碑要做什么

## M0 · 接缝验证 ✅

范围按 G27 收窄：**裸 TCP + 裸 UDP 验 fd 移交，QUIC 推后**。

> 风险本体是「非 Pingora 托管的监听器能否参与 fd 移交」，这与监听器上跑什么协议无关——能移交裸 UDP 的 fd，就能移交 QUIC 的。把 QUIC 排除在外，失败时才能立刻分清是**接缝**的问题还是 **QUIC 库**的问题。

结果与完整证据见 [M0 接缝验证](/verification/m0-seam.md)。

## M1 · 接管一台机

DSL → 结构化配置 → 运行时全链路；全量原子 load API（Unix socket）；自动 HTTPS（含 `.cn` / `.cc` 双域）；反向代理 + 基础负载均衡 + 主动健康检查 + DNS 定期重解析；未实现能力回落。

★ **M1 开工前必须先定 D2–D6**（线程模型、DSL 指令集、ACME、路径约定、回落层进程管理），见 [待定清单](/governance/open-questions.md)。

## M2 · 接管两台

L4 面、静态文件、磁盘缓存、HTTP/3 **依次自研上线并逐块拆除回落**；Runtime 增量通道 + 临时覆盖层；观测三件套。

★ **拆除回落是里程碑的一部分，不是「以后再说」**——见 [回落层](/architecture/fallback.md) 的第 3 条约束。

★ **顺序由 owner 的负载画像定**HTTP 反代 + 静态站、**有非 HTTP 端口要透传**、有重负载需要磁盘缓存 ⇒ **第一块取 L4 面**，它是三样里唯一「今天就有真实需求、且不依赖任何待定项」的。

⚠ **「不挡开工」不等于「可以一直不拍」**M2 开工时 D7 / D8 / D11 都还开着，而 L4 一条都不依赖。★ 但 D8 决定 `pingora-cache` 要不要吃进 fork，**而那件事的代价是依赖图，越晚做越贵**；三条后来都结了案（D8→G82–G84、D11→G103–G105、D7→G113/G114）。

## M3 · 对拍达标

与 Caddy、HAProxy、Nginx **三家同机同用例对拍**，不挑软柿子。详见 [性能验收标准](/verification/performance-bar.md)。

## M4 · 发布

文档、`deb`/`rpm` 包、DSL 参考、`nginx.conf` 与 `Caddyfile` 迁移指南、安全披露流程。

★ **发布前需正式审查三家的许可证义务与打包方式**（`PLAN.md` §9 风险表末条）。

> ⚠ **仓库已经公开了（G115），而这个里程碑没有因此提前。** 「源码看得见」与「可以拿去用」是两件事：现在没有版本号、没有包、没有兼容性承诺。

# ★ 退出条件的共同形状

五个退出条件没有一个是「功能做完了」。它们全是**外部可核验的事实**：

- M0 是**测试的退出码**（`tests/m0/run.sh` 非零即失败）
- M1/M2 是**真实域名在真实流量下跑了多久**
- M3 是**第三方能不能复现那组数字**
- M4 是**生产连续承载的天数**

★ 这不是巧合，是 G24 的直接后果。**「能不能真用」不能由实现者自己宣布。**
