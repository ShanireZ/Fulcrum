# 反向那一半：一份**必须被拒绝**的配置（D22）。
#
# ★ ★ **它存在的理由是「退出码 0」证不了什么**：一个连配置都没读、启动就 `exit(0)`
#   的二进制，在正向那一格里与真的跑完四层校验**完全一样**。
#   ⇒ 这一份要让它**红在一个具体的地方**，而那句话只有真的走过编译层才说得出来。
#
# ★ 取 `fallback_nginx` 是有意的：它不是「打错字」，而是一条**曾经存在、后来删掉**的
#   全局选项（M2 批 G，回落层整块归零），产品为它留了一句专门的诊断
#   （`compile.rs` 的 `removed_global_option`）。⇒ 断言那句话在，就同时证明了：
#   ① DSL 前端跑了；② 编译层跑了；③ 那张「只增不删」的历史表也被编进产物了。
#
# ⚠ ⚠ **这份夹具的第一版是错的，而抓到它的正是那条断言本身。**
#   第一版把 `fallback_nginx 127.0.0.1:9` 写在了顶层 —— 而全局选项要写在
#   **一个位于最前面的 `{ … }` 块里**（DSL 参考 §一），于是解析器把那一行当成了
#   一个站点地址，报的是 `FUL-DSL-0006: 站点块缺少 {`。
#   ★ ★ 它确实被拒了、退出码也确实是 1 —— **如果那条断言只写「退出码非 0」，
#     这一格会是绿的，而我会以为编译层跑过了。**
#   ⇒ 与本仓库反复记的那条同形：**一个更宽的断言会让一个坏夹具悄悄通过。**
{
    fallback_nginx 127.0.0.1:9
}

:8080 {
    respond 200 "should never be reached"
}
