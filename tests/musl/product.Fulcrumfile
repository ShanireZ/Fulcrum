# 给「产品 musl 静态产物」那一格用的最小配置（D22）。
#
# ★ ★ **它刻意只有一个站点、一条 `respond`**：这一格要证的是
#   「**产物是不是单静态二进制、在一个什么都没有的镜像里跑不跑得起来**」，
#   不是产品功能对不对 —— 后者由容器里那些场景管，而它们跑的是 glibc 产物。
#
# ⚠ 但也不能是一份空文件：`fulcrum validate` 跑的是**四层**
#   （诊断 → 结构化 → 运行时图 → TLS 装载），而一份空配置会在第一层就短路，
#   于是这一格实际上只证明了「这个二进制能启动并打印错误」。
#   ⇒ 至少要有一个真的站点块，让运行时图真的被构建一次。
#
# ⚠ **不要在这里加 `tls`**：那会让 TLS 装载那一层去读证书文件，
#   而 `FROM scratch` 里除了这个二进制、这份配置之外什么都没有。
:8080 {
    respond 200 "musl static product ok"
}
